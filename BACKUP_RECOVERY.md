# 💾 BACKUP & RECOVERY — MotorSoft (10.01.2026)

> **Если что-то сломается, вернись сюда!**

---

## 🔐 ТЕКУЩИЙ BACKUP

**Дата:** 10 января 2026 г., 14:45

**Версия:** v1.0-STABLE-10.01.2026

**Размер:** 85 MB

**Файл архива:** `MotorSoft_BACKUP_10.01.2026.tar.gz`

---

## ✅ ЧТО СОХРАНЕНО В ЭТОМ BACKUP:

### 🟢 Полностью рабочие компоненты:
- ✅ Frontend (Next.js) — motorsoft.pro
- ✅ Backend API (FastAPI) — все endpoints
- ✅ Telegram бот (@MotorSoft_bot) — полный функционал
- ✅ Database схема — 7,407 прошивок
- ✅ OCR интеграция (Yandex Vision)
- ✅ Поиск и распознавание файлов

### 🟡 Частично рабочие:
- ⚠️ Балансы и платежи (база есть, механика работает)

### 🔴 Не работающие (но сохранены):
- ❌ Выдача файлов (нет самих файлов на диске)
- ❌ Admin Panel (не реализована)
- ❌ WinOLS интеграция (не реализована)

---

## 🔄 КАК ВЕРНУТЬСЯ К ЭТОМУ BACKUP

### Вариант 1: Git tag (РЕКОМЕНДУЕТСЯ)

```bash
# Посмотреть все теги
git tag -l

# Вернуться к этому backup
git checkout v1.0-STABLE-10.01.2026

# Вернуться на главную ветку
git checkout main
```

**Преимущества:**
- Быстро (не нужно распаковывать)
- Всё сохраняется в git истории
- Можно просмотреть изменения

### Вариант 2: Архив (физический backup)

```bash
# Распаковать архив
tar -xzf MotorSoft_BACKUP_10.01.2026.tar.gz

# Если нужно восстановить в существующую папку
cd "MotorSoft 06.01.26"
tar -xzf ../MotorSoft_BACKUP_10.01.2026.tar.gz --strip-components=1
```

**Преимущества:**
- Физическая копия
- Может быть на другом диске/облаке
- На случай если git сломается

### Вариант 3: Дублирование Docker контейнеров

```bash
# Сохранить текущие образы
docker save motorsoft060126-backend > backend_backup.tar
docker save motorsoft060126-bot > bot_backup.tar

# Восстановить если нужно
docker load < backend_backup.tar
docker load < bot_backup.tar
```

---

## 📋 STEP-BY-STEP ВОССТАНОВЛЕНИЕ

### Если сломался код backend:

```bash
# 1. Посмотри что изменилось
git diff v1.0-STABLE-10.01.2026..HEAD -- backend/

# 2. Вернись к стабильной версии
git checkout v1.0-STABLE-10.01.2026 -- backend/

# 3. Пересобери контейнер
docker build --no-cache -t motorsoft060126-backend backend/
docker restart motorsoft_api
```

### Если сломался код bot:

```bash
# 1. Вернись к стабильной версии
git checkout v1.0-STABLE-10.01.2026 -- bot/

# 2. Пересобери контейнер
docker build --no-cache -t motorsoft060126-bot bot/
docker restart motorsoft_bot
```

### Если сломался frontend:

```bash
# 1. Вернись к стабильной версии
git checkout v1.0-STABLE-10.01.2026 -- frontend/

# 2. Пересобери локально
cd frontend
npm install
npm run build

# 3. Деплой на Vercel (автоматический)
git push  # Vercel автоматически пересоберёт
```

### Если сломалась база данных:

```bash
# ⚠️ ВНИМАНИЕ: БД нельзя восстановить из кода архива!

# Нужно:
# 1. Создать новый dump из рабочей БД
# 2. Восстановить через backup сервиса
# 3. Или переподключиться к резервной копии
```

---

## 🔍 КАК ПРОВЕРИТЬ ЧТО ВСЁ РАБОТАЕТ

После восстановления:

```bash
# 1. Проверить backend
curl http://localhost:8000/

# 2. Проверить бот
docker logs motorsoft_bot --tail 20

# 3. Проверить сайт
open https://motorsoft.pro/

# 4. Проверить API
curl http://localhost:8000/api/v1/api/firmware/stats
```

---

## 📊 КОМПРОМИССЫ ЭТОГО BACKUP

**Плюсы:**
- ✅ Весь рабочий код сохранён
- ✅ Все конфигурации есть
- ✅ Документация полная
- ✅ Git история сохранена

**Минусы:**
- ❌ Нет самих .bin файлов (100+ GB)
- ❌ Нет истории платежей/заказов (в БД)
- ❌ Credentials могут нужны переустановить

---

## 🎯 РЕКОМЕНДАЦИИ

### Если часто меняешь код:
```bash
# Делай тег перед каждым большим изменением
git tag -a "v1.1-feature-xyz" -m "Feature description"
```

### Если нужен полный системный backup:
```bash
# Включи Docker volumes в backup
docker inspect motorsoft_db | grep -i mount
```

### Если работаешь в облаке:
```bash
# Используй облачный backup сервис:
# AWS: S3 + CloudFront
# Yandex: Object Storage + S3 API
# Google: Cloud Storage

# Например, S3:
aws s3 cp MotorSoft_BACKUP_10.01.2026.tar.gz s3://motorsoft-backups/
```

---

## 📞 КОГДА ВОССТАНАВЛИВАТЬ

**Восстанавливай если:**
1. 🔴 Сломался backend
2. 🔴 Сломался бот
3. 🔴 Сломался frontend
4. 🔴 Случайно удалил важный файл
5. 🔴 Новый код не работает и нужно откатиться

**НЕ восстанавливай если:**
1. ✅ Проблемы с БД (нужна отдельная стратегия)
2. ✅ Проблемы с платежами (обратись к ЮKassa)
3. ✅ Проблемы с Telegram API (обратись к Telegram)

---

## 🚀 ПЛАН РЕГУЛЯРНЫХ BACKUP

**Рекомендуемое расписание:**

| Тип | Частота | Где хранить |
|-----|---------|-------------|
| **Git tags** | После каждого релиза | GitHub |
| **Архив проекта** | Еженедельно | Локально + облако |
| **Database dump** | Ежедневно | PostgreSQL backup |
| **Docker images** | После изменения | Docker registry |
| **Credentials** | При изменении | KeePass/защищённое хранилище |

---

## 🔗 ПОЛЕЗНЫЕ КОМАНДЫ

```bash
# Посмотреть все теги
git tag -l

# Посмотреть информацию о теге
git show v1.0-STABLE-10.01.2026

# Создать новый тег
git tag -a "v1.0-NEW" -m "Description"

# Удалить тег (если нужно)
git tag -d v1.0-STABLE-10.01.2026

# Показать разницу между версиями
git diff v1.0-STABLE-10.01.2026..HEAD

# Вернуться только часть файлов
git checkout v1.0-STABLE-10.01.2026 -- backend/
git checkout v1.0-STABLE-10.01.2026 -- bot/

# Создать новую ветку из тега
git checkout -b recovery v1.0-STABLE-10.01.2026
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Git tag находится ТОЛЬКО локально**
   - Для синхронизации: `git push origin v1.0-STABLE-10.01.2026`

2. **Архив НЕ ВКЛЮЧАЕТ:**
   - node_modules (можно переустановить)
   - __pycache__ (пересоздаётся)
   - .venv (можно создать новый)
   - Docker образы (можно пересобрать)

3. **Если восстанавливаешь на новой машине:**
   ```bash
   # 1. Распакуй архив
   tar -xzf MotorSoft_BACKUP_10.01.2026.tar.gz
   
   # 2. Установи зависимости
   cd "MotorSoft 06.01.26"
   cd backend && pip install -r requirements.txt
   cd ../bot && pip install -r requirements.txt
   cd ../frontend && npm install
   
   # 3. Пересобери Docker
   docker-compose up -d --build
   ```

---

> **Создано:** 10 января 2026 г., 14:45
> **Версия:** v1.0-STABLE-10.01.2026
> **Статус:** ✅ Стабильно работает
