# 🛒 План реализации покупки и выдачи прошивок

## ✅ ЧТО УЖЕ ЕСТЬ:

1. **Backend API:**
   - `POST /orders/create` — создание заказа ✅
   - `POST /orders/{order_id}/purchase` — покупка (проверка баланса + списание) ✅
   - `GET /orders/{order_id}` — получение заказа ✅

2. **Bot:**
   - Загрузка файла/скриншота ✅
   - OCR распознавание ✅
   - Поиск прошивки в базе ✅
   - Создание заказа ✅
   - Кнопка "Купить" ✅
   - Callback `confirm_purchase` — обработка покупки ✅

## ❌ ЧТО НЕ РАБОТАЕТ:

### 1. **Нет файлов прошивок для выдачи**
   - В базе есть записи с `software_id`, но нет путей к реальным файлам
   - `order.modified_file_path` = NULL после покупки
   - Бот не может выдать файл потому что его нет

### 2. **Структура хранения файлов не определена**
   - Где хранить 7,407 файлов прошивок?
   - Как их загрузить?
   - Как маппить `firmware_id` → путь к файлу?

### 3. **Нет процесса модификации**
   - Для Stage 1/2/3 нужно модифицировать файл
   - Сейчас система просто выдаёт `modified_file_path` из базы

---

## 📋 ПЛАН ДЕЙСТВИЙ:

### ЭТАП 1: Минимальная работающая версия (MVP)

**Цель:** Выдавать файлы которые УЖЕ ЕСТЬ у клиента

#### Шаг 1.1: Добавить колонку `file_path` в таблицу `firmwares`

```sql
ALTER TABLE firmwares ADD COLUMN file_path VARCHAR(500);
```

**Файлы:**
- `backend/app/models/firmware.py` — добавить поле `file_path`
- `backend/create_tables.py` — обновить миграцию

#### Шаг 1.2: Создать папку для файлов прошивок

```bash
mkdir -p /app/storage/firmwares/
```

**Структура:**
```
storage/
└── firmwares/
    ├── 1.bin        # firmware.id = 1
    ├── 2.bin        # firmware.id = 2
    ├── 2605.bin     # Hyundai Santa Fe 39101-2F310
    └── ...
```

#### Шаг 1.3: Скрипт загрузки файлов от клиента

```python
# scripts/upload_firmwares.py
"""
Клиент загружает архив с прошивками:
firmwares.zip:
  ├── 39101-2F310.bin
  ├── 89663-52N02.bin
  └── ...

Скрипт:
1. Распаковывает архив
2. Для каждого файла ищет firmware по software_id
3. Переименовывает файл в {firmware_id}.bin
4. Копирует в storage/firmwares/
5. Обновляет firmware.file_path в базе
"""
```

#### Шаг 1.4: Обновить backend endpoint покупки

```python
# backend/app/api/endpoints/orders.py
@router.post("/{order_id}/purchase")
async def purchase_order(...):
    # ... проверка баланса, списание ...
    
    # Получаем путь к файлу прошивки
    firmware = await db.get(Firmware, order.firmware_id)
    
    if not firmware.file_path:
        raise HTTPException(
            status_code=404, 
            detail="Файл прошивки не найден на сервере. Обратитесь к оператору."
        )
    
    file_path = f"/app/storage/{firmware.file_path}"
    
    if not os.path.exists(file_path):
        raise HTTPException(
            status_code=404, 
            detail="Файл прошивки отсутствует на диске."
        )
    
    return {
        "status": "ok",
        "price": price,
        "new_balance": user.balance,
        "order_id": order.id,
        "file_path": file_path,  # Полный путь для бота
        "filename": f"{firmware.software_id}.bin"
    }
```

#### Шаг 1.5: Обновить бот для отправки файла

```python
# bot/handlers/upload.py
@router.callback_query(F.data.startswith("confirm_purchase:"))
async def confirm_purchase(callback: CallbackQuery, state: FSMContext):
    result = await api_client.process_purchase(order_id, user_id)
    
    if result.get("error"):
        # ... обработка ошибок ...
        return
    
    # Успех!
    await callback.message.edit_text(
        f"✅ <b>Покупка успешна!</b>\n\n"
        f"💰 Списано: {result['price']} ₽\n"
        f"📁 Отправляю файл..."
    )
    
    # Отправляем файл
    file_path = result.get("file_path")
    filename = result.get("filename", "firmware.bin")
    
    if file_path and os.path.exists(file_path):
        file = FSInputFile(file_path, filename=filename)
        await callback.message.answer_document(
            file,
            caption="📦 <b>Ваша модифицированная прошивка</b>\n\nСпасибо за покупку!"
        )
    else:
        await callback.message.answer(
            "⚠️ Файл временно недоступен. Обратитесь к оператору."
        )
    
    await state.clear()
    await callback.answer("✅ Готово!")
```

---

### ЭТАП 2: Автоматическая модификация (Stage 1/2/3)

**Цель:** Модифицировать файл перед выдачей клиенту

#### Вариант A: WinOLS API (если есть)
```python
# services/winols_api.py
async def modify_firmware(
    stock_file: str,
    stage: int,  # 1, 2, 3
    ecu_type: str
) -> str:
    """
    Вызывает WinOLS API для модификации
    Возвращает путь к модифицированному файлу
    """
```

#### Вариант B: Заранее модифицированные файлы
```
storage/
└── firmwares/
    ├── stock/
    │   └── 2605.bin          # Оригинал
    ├── stage1/
    │   └── 2605_s1.bin       # Stage 1
    ├── stage2/
    │   └── 2605_s2.bin       # Stage 2
    └── stage3/
        └── 2605_s3.bin       # Stage 3
```

#### Вариант C: Ручная обработка оператором
```python
# Если файла нет — создаём заявку оператору
if not firmware.file_path:
    order.status = "manual"
    # Уведомление оператору в Telegram
```

---

## 🎯 ЧТО НУЖНО ОТ КЛИЕНТА:

### 1. **Архив с прошивками**
   Формат: `firmwares.zip`
   ```
   ├── 39101-2F310.bin
   ├── 89663-52N02.bin
   ├── GATA-BE42QS09A00.bin
   └── ... (7,407 файлов)
   ```

### 2. **Соответствие имён файлов и software_id**
   - Имя файла = software_id из базы
   - Или CSV маппинг: `software_id,filename`

### 3. **Способ модификации**
   - WinOLS API доступен?
   - Файлы уже модифицированы?
   - Оператор делает вручную?

### 4. **Структура Stage 1/2/3**
   - Один файл или 3 варианта для каждой прошивки?
   - Разная цена за stage?

---

## ⚡ БЫСТРЫЙ СТАРТ (2 часа работы):

### Вариант 1: Тестовый запуск без реальных файлов

```python
# backend/app/api/endpoints/orders.py
@router.post("/{order_id}/purchase")
async def purchase_order(...):
    # ... проверка, списание ...
    
    # ЗАГЛУШКА: Генерируем пустой файл для теста
    test_file = f"/tmp/firmware_{order_id}.bin"
    with open(test_file, "wb") as f:
        f.write(b"TEST FIRMWARE FILE")
    
    return {
        "file_path": test_file,
        "filename": f"firmware_{firmware.software_id}.bin"
    }
```

### Вариант 2: Загрузить 3-5 реальных файлов для теста

```bash
# 1. Клиент даёт 3 файла:
storage/firmwares/
├── 2605.bin  # Hyundai Santa Fe 39101-2F310
├── 7123.bin  # Toyota Sienta 89663-52N02
└── 2661.bin  # Hyundai Solaris GATA-BE42QS09A00

# 2. Обновляем базу:
UPDATE firmwares SET file_path = 'firmwares/2605.bin' WHERE id = 2605;
UPDATE firmwares SET file_path = 'firmwares/7123.bin' WHERE id = 7123;
UPDATE firmwares SET file_path = 'firmwares/2661.bin' WHERE id = 2661;

# 3. Тестируем покупку через бота
```

---

## 📊 ИТОГО:

**Для работающей покупки нужно:**

1. ✅ Backend API (есть)
2. ✅ Бот с обработкой покупки (есть)
3. ❌ Файлы прошивок на сервере (нет)
4. ❌ Путь к файлам в базе данных (нет)
5. ❌ Логика выдачи файла (простая, 10 строк кода)

**Время реализации:**
- Минимальная версия (выдача готовых файлов): **2 часа**
- С автоматической модификацией: **1-2 дня**
- Полноценная система с Stage 1/2/3: **3-5 дней**

**Следующий шаг:** 
Спросить клиента — **есть ли у него архив с файлами прошивок?**
